import { APP_CONFIG } from './app.config';
import { DATABASE_CONFIG } from './database.config';
import { CRON_CONFIG } from './cron.config';

export const CONFIG = [APP_CONFIG, DATABASE_CONFIG, CRON_CONFIG];
